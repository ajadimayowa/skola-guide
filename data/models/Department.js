class Department {
    constructor(id, icon_name, name_of_department) {
        this.id = id,
            this.icon_name = icon_name,
            this.name_of_department = name_of_department

    }
}

export default Department;